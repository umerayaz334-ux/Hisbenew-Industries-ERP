# Generate a Fernet key and keep the real value only in backend/.env.
# Example generator:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AMAZON_CREDENTIALS_ENCRYPTION_KEY=
